import React, { FC } from "react";

const Header = () => (
  <div className="header flex w-full bg-sky-800 p-4 text-slate-50 font-bold tracking-wide">
    <h2 className="md:text-4xl sm:text-2xl flex-1">Proverbs Talks App</h2>
  </div>
);

export default Header;
