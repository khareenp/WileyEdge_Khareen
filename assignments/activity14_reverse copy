//Khareen Proverbs
// Activity 14

// Write a program that asks the user for an input integer and then computes the reverse of that number

// Example:

// Input: 12456
// Output: 65421


package assignments
import (
	"fmt"
	"strconv"
)

func Reverse() {
    var num1 int
    //prompt user for input
	fmt.Println("Please enter number 1:")
	fmt.Scanf("%d",&num1)
    num2:= strconv.Itoa(num1)//convert number to string

    for i :=len(num2)-1 ; i>=0; i-- {//from last index to first index
      fmt.Printf("%s",string(num2[i]))//print each index
   }

    
}